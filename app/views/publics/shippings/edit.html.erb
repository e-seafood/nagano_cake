<h2>配送先編集</h2>

<%= form_for(@shipping) do |f| %>

	<h4>郵便番号</h4>
	<%= f.text_field :postcode %>

	<h4>住所</h4>
	<%= f.text_field :address %>

	<h4>宛名</h4>
	<%= f.text_field :name %>
	<%= link_to '編集', edit_shipping_path(@shipping.id) %>
<% end %>


