<h3>商品一覧（全X件）</h3>
<% @items.each do |item| %>
  <div>
    <%= link_to item_path(item) do %>
      <%= attachment_image_tag item, :image, size: "150x100" %>
    <% end %>
  </div>
  <p><strong><%= item.name %></strong></p>
  <p><strong>￥<%= item.non_taxed_price %></strong><p>
<% end %>