<% if cart.errors.any? %>
    <div>
      <h4><%= cart.errors.count %>件のエラーが発生しました。</h4>
      <ul>
      <% cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
<% end %>
