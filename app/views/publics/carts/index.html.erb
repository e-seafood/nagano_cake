
<h1 class="page-title">ショッピングカート</h1>
	<div id="result" >

    </div>
<% if @carts.present? %>
  <%= link_to "カートを空にする", cart_destroy_all_path(@carts), method: :delete, data: {confirm: "カートを空にしてよろしいですか？"}, class: "btn btn-danger pull-right" %>
<% end %>
<br><br>
  <div class="cart-items">
    <%= render "publics/carts/update_carts",carts: @carts %>
  </div>

<%= link_to "情報入力に進む", new_order_path, class: "btn btn-success col-md-offset-4 col-md-3" %>
