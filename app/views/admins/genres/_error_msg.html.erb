<% if genre.errors.any? %>
    <div>
      <h4><%= genre.errors.count %>件のエラーが発生しました。</h4>
      <ul>
      <% genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
<% end %>
