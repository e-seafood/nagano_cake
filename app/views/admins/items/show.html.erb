<h1>商品詳細</h1>
<%= attachment_image_tag @item, :image, :fill, 100, 100 %>
<table>
  <tr>
    <td>商品名</td>
    <td><%= @item.name %></td>
  </tr>
  <tr>
    <td>商品説明</td>
    <td><%= @item.explanation %></td>
  </tr>
  <tr>
    <td>ジャンル</td>
    <td><%= @item.genre.name %></td>
  </tr>
  <tr>
    <td>税込価格（税抜価格）</td>
    <td><%= tax(@item.non_taxed_price).to_i(:delimited) %>（<%= @item.non_taxed_price.to_i(:delimited) %>）円</td>
  </tr>
  <tr>
    <td>ステータス</td>
    <td>
      <% if @item.is_active == true %>
        <p>販売中</p>
      <% else %>
        <p>売切れ</p>
      <% end %>
    </td>
  </tr>
</table>
<%= link_to "編集する", edit_admins_item_path, class: "btn btn-primary pull-right" %>